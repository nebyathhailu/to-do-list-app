{
  "provider": "node",
  "phases": {
    "install": {
      "cmds": ["npm install"]
    },
    "build": {
      "cmds": ["vite build"]
    }
  },
  "start": {
    "cmd": "npx serve -s dist -l $PORT"
  }
}