{
  "provider": "node",
  "phases": {
    "install": {
      "cmds": ["npm install"]
    },
    "build": {
      "cmds": ["vite build"]
    }
  },
  "start": {
    "cmd": "npx vite preview --host 0.0.0.0 --port $PORT"
  }
}